### 什么策略模式? 

定义一系列算法，把它们一个个封装起来，并且使它们可以互相替换，目的就是将算法的使用和实现分离开来

### 使用场景有哪些？

1、弹出框，一般页面只会存在一个

2、提供一个对象对外暴露方法, 通常使用闭包防止命名空间污染，但是在es6的let const 声明和module中通常不需要考虑

````typescript

let obj = {
  getData() {},
  setData() {}
}

```